image: python:3.8-slim

pages:
  script:
  - pip install sphinx
  - pip install sphinx_rtd_theme
  - pip install -r requirements.txt
  - sphinx-build build-docs/ docs/
  artifacts:
    paths:
    - public
  only:
  - main
