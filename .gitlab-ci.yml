image: python:3.8-slim

pages:
  script:
  - pip install -r requirements.txt
  - sphinx-build -d build-docs/ . docs/html
  - mv docs/html public
  artifacts:
    paths:
    - public
  only:
  - master